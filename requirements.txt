# Runtime dependencies for the analysis, figures and tests.
# Versions are lower bounds; the versions actually used are recorded in the
# "environment" block of output/results.json.

numpy>=1.24
pandas>=2.0
matplotlib>=3.7
PyYAML>=6.0

# Required only by scripts/00_download_isc.py, which retrieves the ISC exports.
# The analysis itself runs from the released data files without it.
requests>=2.28

# Optional. The test suite also runs without pytest, via
# python tests/run_tests.py
# pytest>=7.0
